apiVersion: v1
kind: Secret
metadata:
  name: git-manage-secret
  namespace: default
type: Opaque
stringData:
  # Sensitive data should be base64 encoded in real usage, or use stringData for convenience
  DB_PASSWORD: "gitpassword"
  WEBHOOK_SECRET: "my-secret-key"
